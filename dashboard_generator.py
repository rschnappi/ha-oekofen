import os

# Get the directory of the current script
current_dir = os.path.dirname(os.path.abspath(__file__))

# Updated path to sensor.py
sensor_path = os.path.join(current_dir, 'sensor.py')

# Rest of the dashboard_generator.py code
