name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          # homeassistant 2026.8.2 requires Python 3.14+.
          python-version: "3.14"

      - name: Install test dependencies
        run: pip install -r requirements-test-ha.txt

      - name: Run tests
        run: pytest tests/ -v

      - name: Compile check integration files
        run: python -m py_compile custom_components/oekofen/*.py
