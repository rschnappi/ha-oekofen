pytest>=7.4
pytest-asyncio>=0.21
# No standalone aiohttp/aioresponses pins here on purpose: aioresponses
# patches aiohttp's ClientResponse internals directly and is broken as of
# aiohttp>=3.10 (confirmed: TypeError: ClientResponse.__init__() missing
# 'stream_writer'), which previously forced an aiohttp<3.10 cap that fought
# with homeassistant's own exact aiohttp pin and blocked using any
# non-vulnerable homeassistant release. test_pellematic_api.py now drives
# PellematicAPI's real aiohttp.ClientSession against a real local
# aiohttp.web server (aiohttp.test_utils.TestServer) instead, so it only
# depends on aiohttp's public web/client APIs and floats freely with
# whatever aiohttp version homeassistant (or a bare `pip install aiohttp`)
# resolves.
aiohttp
async-timeout>=4.0
