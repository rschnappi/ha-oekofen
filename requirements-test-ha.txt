# Additional requirements for testing the number/select/switch/climate/
# time/datetime platforms, which subclass real Home Assistant entity base
# classes. requirements-test.txt alone (no homeassistant) is enough for
# test_pellematic_api.py, which has no Home Assistant dependency at all.
-r requirements-test.txt
# Pinned (not left floating) so pip doesn't have to resolve a version -
# an unpinned "homeassistant" makes it search the entire release history,
# which previously landed on pre-2021 releases whose transitive deps (old
# PyYAML) don't build on modern Python and broke CI. Track the same release
# the live instance actually runs (see README) so this also catches real
# API drift instead of testing against an increasingly-stale mock.
homeassistant==2026.8.2
